# euidaeguard-backend

## 커밋 메시지 규칙

커밋을 요청받으면 **Conventional Commits 형식 + 한국어 본문**으로 작성한다.

```
<type>: <한국어로 무엇을 왜 바꿨는지 한 줄 요약>
```

- 타입은 영어 소문자로 고정: `feat`, `fix`, `chore`, `docs`, `refactor`, `style`, `test`, `perf`, `build`, `ci`
- 콜론 뒤 제목은 **한국어**로 쓴다. (영어 제목 금지)
- 제목은 50자 내외, 마침표 없이 끝낸다.
- 필요하면 빈 줄 뒤에 한국어로 본문(상세 설명)을 덧붙인다.

타입 선택 기준:

| 타입 | 사용 시점 |
| --- | --- |
| `feat` | 새 기능, 새 엔드포인트, 새 파이프라인 추가 |
| `fix` | 버그 수정, 잘못된 동작 교정 |
| `chore` | 의존성/설정/스크립트 등 코드 외 잡일 |
| `docs` | 문서·지식소스 문서만 변경 |
| `refactor` | 동작 변화 없는 구조 개선 |
| `style` | 포맷팅, 공백 등 |
| `test` | 테스트 추가·수정 |
| `perf` | 성능 개선 |

예시:

```
feat: 상담 인테이크 정보가 없을 때 발화로 문제 유형 추론
fix: 무음 구간에서 발생하는 STT 환각 문장 제거
chore: requirements.txt 의존성 버전 정리
docs: 지식베이스 동작 메커니즘 문서 추가
refactor: 인용 검증 로직을 별도 모듈로 분리
```
